{
  "name": "sourcesnap",
  "version": "1.0.0",
  "description": "Alt+Click any element to open its source in your editor",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "typescript": "^5.3.3",
    "vite": "^5.0.12"
  },
  "dependencies": {
    "canvas": "^3.2.1"
  }
}
